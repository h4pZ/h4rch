-- Python
local dap_install = require("dap-install")
dap_install.config("python_dbg", {
        -- configurations = {
        --     pythonPath = function()
        --         return vim.fn.executable("!which python")
        --     end
        -- }
    })

